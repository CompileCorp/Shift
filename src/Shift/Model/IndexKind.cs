namespace Compile.Shift.Model
{
    public enum IndexKind
    {
        NonClustered,
        Clustered,
        ClusteredColumnstore,
        NonClusteredColumnstore,
        FullText,
        Xml,
        Spatial
    }
}